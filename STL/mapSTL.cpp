/*
Numeric Array
    If the index is represented by numbers then it is a numeric array.
    
Associative arrays
    if the index is represented by anything but numbers then it is an associative array.
    index means we associate a value in the array with something by the help of which, we can access the value.
*/
/*
map
    maps are used to replicate associative arrays
    maps contain sorted key-value pair, in which each key is unique and cannot be changed, and it can be inserted or deleted but cannot
    be altered. key ~ index
*/